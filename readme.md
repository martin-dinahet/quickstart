# Quickstart

This repository contains multiple starter templates.
Each template is located in its own directory

## Usage

To create a new project from a specific template, use `degit`.
```
pnpm add --global degit
degit github:martin-dinahet/quickstart/<template-folder> <project-name>
```

Each template has its own `readme.md` describing stack and usage.